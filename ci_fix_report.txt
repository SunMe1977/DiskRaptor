DiskRaptor CI Fix Report
=========================
Date: 2026-07-13
Previous tag: v0.2.2 (failed)
Fix tag:      v0.2.3 (pushed to trigger verification)

Failures Identified
-------------------
The v0.2.2 CI run failed in all 3 build jobs:

1. build-app (Windows - NSIS installer)
   - Step "Install NSIS (portable)" failed
   - Root cause: PowerShell Expand-Archive command had trailing backslash 
     before closing quote: C:\nsis\" — the \" escaped the quote, causing 
     "The string is missing the terminator" parser error
   - Fix: Changed destination to C:\nsis (no trailing backslash)

2. build-macos (macOS - Qt install)
   - Step "Install Qt" failed
   - Root cause: macos-latest now points to ARM64 runner, but 
     clang_arm64 arch doesn't have Qt 6.10.3 packages available
   - Error: "The packages ['qt_base', 'qtpositioning', 'qtwebchannel', 
     'qtwebengine'] were not found while parsing XML"
   - Fix: Pinned runner to macos-13 (x64) with arch clang_64

3. build-linux (Linux - linuxdeployqt)
   - Step "Deploy Qt libs (linuxdeployqt)" failed
   - Root cause: images/256x256.png not tracked in git (images/ is 
     gitignored with "# Generated icon copies")
   - Fix: Made icon copy conditional (only copies if file exists);
     force-added 256x256.png + other icon assets to git

4. release (cascade failure)
   - All 3 upstream builds failed, no artifacts uploaded → release 
     job failed at "Prepare assets" (no artifacts to prepare)

Changes Committed
-----------------
Commit 0d60998 on main:
  - .github/workflows/build.yml: NSIS quoting fix, macOS runner pin,
    Linux conditional icon copy
  - images/256x256.png, 128x128.png, 32x32.png, icon.icns: Added 
    to git (force-added past gitignore)

Tag v0.2.3 pushed to trigger verification run.
