name: Build and Release

on:
  push:
    branches: [main, master, develop]
    tags: ["v*"]
  pull_request:
    branches: [main, master]

env:
  RUST_BACKTRACE: 1
  CARGO_TERM_COLOR: always

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install lint dependencies
        run: |
          sudo apt-get update -qq
          if apt-cache show libwebkit2gtk-4.1-dev >/dev/null 2>&1; then
            sudo apt-get install -y -qq libwebkit2gtk-4.1-dev
          elif apt-cache show libwebkit2gtk-4.0-dev >/dev/null 2>&1; then
            sudo apt-get install -y -qq libwebkit2gtk-4.0-dev
          else
            echo "No suitable WebKit2GTK dev package found" >&2
            exit 1
          fi

          sudo apt-get install -y -qq \
            libgtk-3-dev \
            librsvg2-dev \
            libglib2.0-dev \
            pkg-config
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy, rustfmt
      - run: cargo fmt --all --check
      - run: cargo clippy --manifest-path src-tauri/Cargo.toml

  # ── Windows Build ─────────────────────────────────────
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - name: Install Node dependencies
        run: |
          if (Test-Path package-lock.json) {
            npm ci
          } else {
            npm install
          }
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Build binary
        run: cargo build --manifest-path src-tauri/Cargo.toml --release
      - name: Bundle MSI
        run: |
          cd src-tauri
          npx tauri build --bundles msi --ci --verbose
      - uses: actions/upload-artifact@v4
        with:
          name: release-Windows
          compression-level: 0
          path: |
            src-tauri/target/release/diskraptor.exe
            src-tauri/target/release/bundle/msi/**/*.msi
          if-no-files-found: error
          retention-days: 7

  # ── macOS Build ───────────────────────────────────────
  build-macos:
    name: Build macOS
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - name: Install Node dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Build binary
        run: cargo build --manifest-path src-tauri/Cargo.toml --release
      - name: Bundle DMG
        run: |
          cd src-tauri
          npx tauri build --bundles dmg --ci --verbose
      - uses: actions/upload-artifact@v4
        with:
          name: release-macOS
          compression-level: 0
          path: |
            src-tauri/target/release/diskraptor
            src-tauri/target/release/bundle/dmg/**/*.dmg
          if-no-files-found: error
          retention-days: 7

  # ── Linux Build ───────────────────────────────────────
  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - name: Install Node dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Install Linux dependencies
        run: |
          sudo apt-get update -qq

          # Tauri v1 may require either 4.0 or 4.1 depending on runner image.
          if apt-cache show libwebkit2gtk-4.1-dev >/dev/null 2>&1; then
            sudo apt-get install -y -qq libwebkit2gtk-4.1-dev
          elif apt-cache show libwebkit2gtk-4.0-dev >/dev/null 2>&1; then
            sudo apt-get install -y -qq libwebkit2gtk-4.0-dev
          else
            echo "No suitable WebKit2GTK dev package found" >&2
            exit 1
          fi

          sudo apt-get install -y -qq \
            libgtk-3-dev \
            librsvg2-dev \
            patchelf \
            libayatana-appindicator3-dev \
            libappindicator3-dev

          if apt-cache show libfuse2 >/dev/null 2>&1; then
            sudo apt-get install -y -qq libfuse2
          elif apt-cache show libfuse2t64 >/dev/null 2>&1; then
            sudo apt-get install -y -qq libfuse2t64
          else
            echo "No suitable libfuse2 package found" >&2
            exit 1
          fi
      - name: Build binary
        run: cargo build --manifest-path src-tauri/Cargo.toml --release
      - name: Bundle AppImage
        run: |
          cd src-tauri
          npx tauri build --bundles appimage --ci --verbose
      - uses: actions/upload-artifact@v4
        with:
          name: release-Linux
          compression-level: 0
          path: |
            src-tauri/target/release/diskraptor
            src-tauri/target/release/bundle/appimage/**/*.AppImage
          if-no-files-found: error
          retention-days: 7

  # ── Create GitHub Release (only for tags) ─────────────────
  release:
    name: Create Release
    if: github.ref_type == 'tag'
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: release-Windows
          path: artifacts/windows

      - uses: actions/download-artifact@v4
        with:
          name: release-macOS
          path: artifacts/macos

      - uses: actions/download-artifact@v4
        with:
          name: release-Linux
          path: artifacts/linux
          

      - name: Prepare release assets
        run: |
          mkdir -p release_assets
          echo "=== All downloaded files ==="
          find artifacts -type f 2>/dev/null | sort

          for f in $(find artifacts -type f); do
            [ ! -f "$f" ] && continue
            base=$(basename "$f")
            case "$base" in
              *.msi)
                cp "$f" release_assets/
                echo "  [Windows] $base"
                ;;
              *.dmg)
                cp "$f" release_assets/DiskRaptor-macOS.dmg 2>/dev/null || cp "$f" release_assets/
                echo "  [macOS] dmg"
                ;;
              *.AppImage)
                cp "$f" release_assets/DiskRaptor-Linux.AppImage 2>/dev/null || cp "$f" release_assets/
                echo "  [Linux] AppImage"
                ;;
              diskraptor.exe)
                cp "$f" release_assets/DiskRaptor-Windows-x64.exe
                echo "  [Windows] exe"
                ;;
              diskraptor)
                chmod +x "$f"
                if [[ "$(dirname "$f")" == *macos* ]] || [[ "$(dirname "$f")" == *osx* ]]; then
                  cp "$f" release_assets/DiskRaptor-macOS-x64
                  echo "  [macOS] binary"
                else
                  cp "$f" release_assets/DiskRaptor-Linux-x64
                  echo "  [Linux] binary"
                fi
                ;;
            esac
          done

      - name: Validate required release files
        run: |
          ls -lh release_assets/

          test -f release_assets/DiskRaptor-Windows-x64.exe
          test -n "$(find release_assets -maxdepth 1 -name '*.msi' -print -quit)"
          test -n "$(find release_assets -maxdepth 1 -name '*.dmg' -print -quit)"
          test -n "$(find release_assets -maxdepth 1 -name '*.AppImage' -print -quit)"

          echo "All required artifacts are present (.exe, .msi, .dmg, .AppImage)."

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: DiskRaptor ${{ github.ref_name }}
          body: |
            ## DiskRaptor ${{ github.ref_name }}

            | Platform | Downloads |
            |----------|-----------|
            | **Windows** | `.msi` installer or `DiskRaptor-Windows-x64.exe` |
            | **macOS** | `*.dmg` or `DiskRaptor-macOS-x64` binary |
            | **Linux** | `*.AppImage` or `DiskRaptor-Linux-x64` binary |

            > ⚠️ macOS & Linux builds are unsigned.
          files: release_assets/**
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
