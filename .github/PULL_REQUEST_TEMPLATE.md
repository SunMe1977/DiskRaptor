## What does this change?

## Why? (link issue or describe the user benefit)

## Verification

- [ ] `node scripts/check-version.mjs` (if version touched)
- [ ] Relevant tests run (see `package.json` scripts / `test.sh --quick`)
- [ ] Screenshots for UI changes (before/after)

## Checklist

- [ ] No unrelated changes
- [ ] Docs/README updated if user-facing behavior changed
