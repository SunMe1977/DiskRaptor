# ── Build artifacts ─────────────────
target/
qt-app/build*/
dist/
dist-mas/
release/
*.msi
*.exe
*.AppImage
*.deb
*.dmg
*.obj
*.o
*.so
*.lib
*.exp
*.dll

# ── Node ────────────────────────────
node_modules/
frontend/lib/
package-lock.json

# ── IDE ─────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.suo
*.user

# ── OS ──────────────────────────────
.DS_Store
Thumbs.db

# ── Temp / Debug ────────────────────
*.log
linuxcrash.txt
macerror.txt
crash*.txt
*.py
*.ps1
*.bat
!build.cmd
test-results/

# ── Qt runtime (launcher downloads) ─
qtwebengine_runtime/
qtwebengine_*.zip

# ── Pro Modules (closed source) ─────
modulesPro/*.dll
modulesPro/*.so
modulesPro/*.dylib
modulesPro/*.lib
modulesPro/*.exp
modulesPro/*/build/
modulesPro/build/

# ── Secrets (NEVER commit) ──────────
.env
.env.*
!.env.example
apple_private.key
developer.key
*.certSigningRequest

# ── Generated / sensitive ───────────
raw/
src-tauri/gen/schemas/
release-assets/
install/
setup.nsi
nul
DiskRaptor-windows.zip
*.zip

# ── DEB package build directory ────────
# deb/ is stale — CI and build.sh recreate it from scratch
deb/


